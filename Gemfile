source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.2'

# Use PostgreSQL as the database for Active Record
gem 'pg'

# Use Foreman so that sensitive environment variables can be loaded from an .env file
gem 'foreman'

# Use HAML, not religiously, but where it lends itself to nice clean markup
gem 'haml-rails'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

# Use Foundation to make everything beautiful
gem 'foundation-rails'

# Use RedCarpet to render Markdown (if applicable)
gem 'redcarpet'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

# Use ActiveModel has_secure_password
gem 'bcrypt-ruby', '~> 3.1.2'

# ...but also use OAuth, for users who roll like that
gem 'omniauth'

# Uncomment any of these you want to use
# gem 'omniauth-google-oauth2'
# gem 'omniauth-facebook'
# gem 'omniauth-github'

# Oh look, somebody else already did this for me
gem 'email_validator'

# Use strip_attributes to get rid of leading and trailing space
gem 'strip_attributes'

# Use unicorn as the app server
gem 'unicorn'

group :development, :test do
  gem 'rspec-rails', '~> 3.0.0.beta'
end

group :production do
  gem 'rails_12factor'
end
